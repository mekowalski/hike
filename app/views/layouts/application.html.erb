<!DOCTYPE html>
<html lang="en">
<head>
  <title>Hike App</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= javascript_include_tag 'application' %>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  <style>

    .navbar {
      background-color: #2F4F4F;
      color: white;
      margin-bottom: 15px;
      border-radius: 0;
    }

    .row.content {height: 450px}

    .sidenav {
      padding: auto;
      background-color: #f1f1f1;
      height: auto;
    }

    .resize {
      width: 1350px;
      height: auto;
    }

    .resize {
      width: 1250px;
      height: auto;
    }

    .field_with_errors input[type='text']{

    }

    footer {
      background-color: #2F4F4F;
      color: #00BCE1;
      padding: 15px;
      margin-top: 15px;
    }

    @media screen and (max-width: 767px) {
      .sidenav {
        height: auto;
        padding: 15px;
      }
      .row.content {height:auto;}
      }

  </style>

</head>
<body>

<nav class="navbar navbar-inverse">
  <div class="container-fluid">

    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <% if current_hiker %>
        <li><%= link_to "View Your Profile", hiker_path(current_hiker.id), class: "nav-links", data: {id: current_hiker.id} %></li>
        <li><%= link_to "All Adventures", adventures_path, id: "all-adventures", class: "nav-links", data: {id: current_hiker.id}%></li>
        <li> <a href="#"><%= notice if notice %></a></li>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if current_hiker %>
        <li><%= link_to "Sign Out", signout_path, class: "nav-links" %></li>
        <% else %>
        <li><%= link_to "Sign In", signin_path, class: "nav-links" %></li>
        <li><%= link_to "Sign Up", signup_path, class: "nav-links" %></li>
        <% end %>
      </ul>
    </div>

  </div>
</nav>

<div class="container-fluid text-center">
  <%= yield %>
</div>

<footer class="container-fluid text-center">
  <p>made with <3 </p>
</footer>

</body>
</html>
